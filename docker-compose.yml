version: '3.9'

services:
  api:
    build: .
    container_name: auth-api
    env_file:
      - .env
    ports:
      - "${PORT:-4000}:4000"
    # No db service here – app connects directly to Supabase using DATABASE_URL
